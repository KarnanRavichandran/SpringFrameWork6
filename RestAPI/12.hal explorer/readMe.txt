We can explore the rest api more using hal explorer,

Add dependency,
   	<dependency>
		<groupId>org.springframework.data</groupId>
		<artifactId>spring-data-rest-hal-explorer</artifactId>
	</dependency>

Go to 8080.

Links/ResponseBody - more like a actuator GUI 